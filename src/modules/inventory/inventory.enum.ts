export enum InventoryType {
  RESERVE = "RESERVE",
  RELEASE = "RELEASE",
  DEDUCT = "DEDUCT",
  ADJUST = "ADJUST",
}

export enum InventoryReferenceType {
  ORDER = "ORDER",
  MANUAL = "MANUAL",
  IMPORT = "IMPORT",
  REFUND = "REFUND",
}
